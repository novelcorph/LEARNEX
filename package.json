{
  "name": "learnex-ai",
  "version": "3.0.0",
  "private": true,
  "description": "Learnex AI educational chat app using the OpenAI Responses API.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node --check server.js && node --check app.js",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "openai": "^5.12.0"
  }
}
