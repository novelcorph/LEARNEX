LEARNEX AI — PWA PATCH

Replace/upload these files at the repository ROOT:
- index.html (replace the existing one)
- manifest.json (new)
- sw.js (new)
- icon-192.png (new)
- icon-512.png (new)

Do NOT delete or replace app.js, styles.css, server.js, package.json, or the existing Learnex images.

After committing, redeploy on Render, wait for the deployment to finish, then run the Learnex URL through PWABuilder again.

The manifest and service worker are configured for an English-only interface. They do not change the AI's Bengali/English response behavior.
