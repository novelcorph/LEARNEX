LEARNEX AI — LOGO REPLACEMENT

Replace these existing files in the GitHub repository root:
1. learnex-icon.png
2. icon-192.png
3. icon-512.png

Do NOT delete server.js, app.js, index.html, styles.css, manifest.json, package.json, sw.js, or other project files.

After replacing the three PNG files:
1. Commit the changes in GitHub.
2. In Render, deploy the latest commit.
3. Wait for the deployment to finish.
4. Re-test the site/PWA.

The existing manifest.json already points to /icon-192.png and /icon-512.png,
and index.html already points to /learnex-icon.png, so no code-file change is
needed just for this logo replacement.
